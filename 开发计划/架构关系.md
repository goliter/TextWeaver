## 总的架构关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           新架构：文件系统归属工作流                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  用户                                                                        │
│  ────                                                                       │
│    │                                                                        │
│    ├──▶ 工作流 1 ──▶ 独立文件系统 1                                          │
│    │         │           │                                                  │
│    │         │           ├── 📁 配置文件/                                    │
│    │         │           │     📄 世界观.md                                  │
│    │         │           │     📄 人物.md                                    │
│    │         │           │                                                  │
│    │         │           └── 📁 章节文件/                                    │
│    │         │                 📄 第1章.md                                   │
│    │         │                                                              │
│    │         └── 工作流节点（引用文件系统 1 中的文件）                          │
│    │                                                                        │
│    ├──▶ 工作流 2 ──▶ 独立文件系统 2                                          │
│    │         │           │                                                  │
│    │         │           ├── 📁 模板/                                        │
│    │         │           │     📄 广告文案.md                                │
│    │         │           │                                                  │
│    │         │           └── 📁 输出/                                        │
│    │         │                 📄 结果1.md                                   │
│    │         │                                                              │
│    │         └── 工作流节点（引用文件系统 2 中的文件）                          │
│    │                                                                        │
│    └──▶ 工作流 3 ──▶ 独立文件系统 3                                          │
│              │                                                              │
│              └── ...                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 数据库设计更新

### 实体关系图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   users     │       │   flows     │       │   files     │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │◀──┐   │ id          │◀──┐   │ id          │
│ username    │   │   │ name        │   │   │ flow_id     │──▶ flows.id
│ email       │   │   │ description │   │   │ parent_id   │──▶ files.id
│ password    │   │   │ owner_id    │───┘   │ name        │
│ ...         │   │   │ nodes       │       │ type        │
└─────────────┘   │   │ edges       │       │ content     │
                  │   │ created_at  │       │ created_at  │
                  │   │ updated_at  │       │ updated_at  │
                  │   └─────────────┘       └─────────────┘
                  │
                  └─── 一个用户可以有多个工作流
                                      
                  ┌─────────────┐       ┌─────────────┐
                  │   flows     │◀──────│ flow_templates│
                  ├─────────────┤       ├─────────────┤
                  │             │       │ id          │
                  │             │       │ name        │
                  │             │       │ nodes       │
                  │             │       │ edges       │
                  │             │       │ files       │──▶ 文件快照
                  │             │       │ author_id   │
                  └─────────────┘       └─────────────┘
```

### 更新后的表结构

```sql
-- 用户表（已有）
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 工作流表
CREATE TABLE flows (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    owner_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    
    -- 工作流定义
    nodes JSONB NOT NULL DEFAULT '[]',
    edges JSONB NOT NULL DEFAULT '[]',
    
    -- 元数据
    is_active BOOLEAN DEFAULT true,
    last_executed_at TIMESTAMP WITH TIME ZONE,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_flows_owner_id (owner_id)
);

-- 文件表（关键变化：归属工作流）
CREATE TABLE files (
    id SERIAL PRIMARY KEY,
    flow_id INTEGER NOT NULL REFERENCES flows(id) ON DELETE CASCADE,
    parent_id INTEGER REFERENCES files(id) ON DELETE CASCADE,
    name VARCHAR(255) NOT NULL,
    type VARCHAR(20) NOT NULL,  -- 'file' | 'folder'
    content TEXT,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_files_flow_id (flow_id),
    INDEX idx_files_parent_id (parent_id)
);

-- 工作流模板表
CREATE TABLE flow_templates (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    author_id INTEGER REFERENCES users(id),
    
    -- 工作流定义
    nodes JSONB NOT NULL,
    edges JSONB NOT NULL,
    
    -- 文件快照（包含文件内容）
    files JSONB NOT NULL DEFAULT '[]',
    
    -- 元数据
    category VARCHAR(100),
    tags TEXT[],
    use_count INTEGER DEFAULT 0,
    is_public BOOLEAN DEFAULT true,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- 执行记录表
CREATE TABLE executions (
    id SERIAL PRIMARY KEY,
    flow_id INTEGER NOT NULL REFERENCES flows(id) ON DELETE CASCADE,
    status VARCHAR(20) NOT NULL,  -- 'pending' | 'running' | 'success' | 'error'
    result JSONB,
    logs JSONB,
    error_message TEXT,
    
    started_at TIMESTAMP WITH TIME ZONE,
    completed_at TIMESTAMP WITH TIME ZONE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_executions_flow_id (flow_id)
);
```

---

## SQLAlchemy 模型更新

```python
# app/modules/flows/models.py
from sqlalchemy import Column, Integer, String, Text, Boolean, ForeignKey, DateTime, JSON
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from app.core.database import Base

class Flow(Base):
    __tablename__ = "flows"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(255), nullable=False)
    description = Column(Text)
    owner_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    nodes = Column(JSON, default=list)
    edges = Column(JSON, default=list)
    is_active = Column(Boolean, default=True)
    last_executed_at = Column(DateTime(timezone=True))
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())

    # 关系
    owner = relationship("User", backref="flows")
    files = relationship("File", back_populates="flow", cascade="all, delete-orphan")
    executions = relationship("Execution", back_populates="flow", cascade="all, delete-orphan")


# app/modules/files/models.py
class File(Base):
    __tablename__ = "files"

    id = Column(Integer, primary_key=True, index=True)
    flow_id = Column(Integer, ForeignKey("flows.id", ondelete="CASCADE"), nullable=False)
    parent_id = Column(Integer, ForeignKey("files.id", ondelete="CASCADE"), nullable=True)
    name = Column(String(255), nullable=False)
    type = Column(String(20), nullable=False)  # 'file' | 'folder'
    content = Column(Text, nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())

    # 关系
    flow = relationship("Flow", back_populates="files")
    parent = relationship("File", remote_side=[id], backref="children")


# app/modules/templates/models.py
class FlowTemplate(Base):
    __tablename__ = "flow_templates"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(255), nullable=False)
    description = Column(Text)
    author_id = Column(Integer, ForeignKey("users.id"))
    nodes = Column(JSON, nullable=False)
    edges = Column(JSON, nullable=False)
    files = Column(JSON, default=list)  # 文件快照
    category = Column(String(100))
    tags = Column(JSON, default=list)
    use_count = Column(Integer, default=0)
    is_public = Column(Boolean, default=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())

    # 关系
    author = relationship("User", backref="templates")
```

---

## API 设计更新

### 工作流 API

| 方法 | 路径 | 描述 |
|-----|------|-----|
| GET | `/api/flows` | 获取用户的所有工作流 |
| POST | `/api/flows` | 创建新工作流 |
| GET | `/api/flows/{flow_id}` | 获取工作流详情 |
| PUT | `/api/flows/{flow_id}` | 更新工作流 |
| DELETE | `/api/flows/{flow_id}` | 删除工作流 |
| POST | `/api/flows/{flow_id}/execute` | 执行工作流 |
| POST | `/api/flows/{flow_id}/save-as-template` | 保存为模板 |

### 文件 API（归属工作流）

| 方法 | 路径 | 描述 |
|-----|------|-----|
| GET | `/api/flows/{flow_id}/files` | 获取工作流的文件树 |
| POST | `/api/flows/{flow_id}/files` | 创建文件/文件夹 |
| GET | `/api/flows/{flow_id}/files/{file_id}` | 获取文件详情 |
| PUT | `/api/flows/{flow_id}/files/{file_id}` | 更新文件 |
| DELETE | `/api/flows/{flow_id}/files/{file_id}` | 删除文件 |

### 模板 API

| 方法 | 路径 | 描述 |
|-----|------|-----|
| GET | `/api/templates` | 获取模板列表 |
| POST | `/api/templates` | 创建模板 |
| GET | `/api/templates/{template_id}` | 获取模板详情 |
| POST | `/api/templates/{template_id}/use` | 使用模板创建工作流 |
| DELETE | `/api/templates/{template_id}` | 删除模板 |

---

## 前端页面结构更新

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  AI 文本工作流                                           用户名 ▼  退出      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📋 我的工作流                              [+ 新建工作流]            │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                     │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │   │
│  │  │ 📖 小说创作  │  │ 📝 文案生成  │  │ 📊 报告总结  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ 12 个文件   │  │ 5 个文件    │  │ 8 个文件    │                │   │
│  │  │ 上次: 2小时前│  │ 上次: 1天前  │  │ 上次: 3天前  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ [打开] [删除]│  │ [打开] [删除]│  │ [打开] [删除]│                │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📚 模板库                                                           │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                     │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │   │
│  │  │ 📖 小说创作  │  │ 📝 营销文案  │  │ 📊 会议纪要  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ 作者: 张三  │  │ 作者: 李四  │  │ 作者: 王五  │                │   │
│  │  │ 使用: 128次 │  │ 使用: 56次  │  │ 使用: 89次  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ [使用模板]  │  │ [使用模板]  │  │ [使用模板]  │                │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 工作流编辑器页面

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  📖 小说创作工作流                              [保存] [执行] [保存为模板]   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────┐  ┌─────────────────────────────────────────────────────┐ │
│  │              │  │                                                     │ │
│  │  📁 文件系统  │  │              React Flow 画布                         │ │
│  │  (此工作流)   │  │                                                     │ │
│  │              │  │   ┌─────────┐     ┌─────────┐     ┌─────────┐       │ │
│  │  📁 配置文件  │  │   │📄 文件  │────▶│🤖 AI   │────▶│📝 文件  │       │ │
│  │    📄世界观   │  │   │ Reader  │     │ Node   │     │ Writer  │       │ │
│  │    📄人物    │  │   └─────────┘     └─────────┘     └─────────┘       │ │
│  │  📁 章节文件  │  │                                                     │ │
│  │    📄第1章   │  │                                                     │ │
│  │              │  │                                                     │ │
│  └──────────────┘  └─────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  属性面板 (选中文件/节点时显示)                                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 更新开发计划文档

我来更新虚拟文件系统的开发计划文档：
        
            
toolName: view_files
            
status: success
          
            
filePath: e:\My Project\ai-work-flow\TextWeaver\开发计划\虚拟文件系统.md
          
现在让我根据新的架构关系更新开发计划文档：

```markdown e:\My Project\ai-work-flow\TextWeaver\开发计划\虚拟文件系统.md
# 虚拟文件系统开发计划

## 一、功能概述

### 1.1 核心目标
构建一个虚拟文件系统，**归属工作流**，每个工作流拥有独立的文件系统。支持用户在浏览器中创建、编辑、管理文件和文件夹，文件内容存储在数据库中，支持与工作流节点进行交互。

### 1.2 核心设计原则

```
用户 1:N 工作流 1:1 文件系统

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  用户                                                                        │
│  ────                                                                       │
│    │                                                                        │
│    ├──▶ 工作流 1 ──▶ 独立文件系统 1                                          │
│    │         │           │                                                  │
│    │         │           ├── 📁 配置文件/                                    │
│    │         │           │     📄 世界观.md                                  │
│    │         │           │     📄 人物.md                                    │
│    │         │           │                                                  │
│    │         │           └── 📁 章节文件/                                    │
│    │         │                 📄 第1章.md                                   │
│    │         │                                                              │
│    │         └── 工作流节点（引用文件系统 1 中的文件）                          │
│    │                                                                        │
│    ├──▶ 工作流 2 ──▶ 独立文件系统 2                                          │
│    │         │                                                              │
│    │         └── ...                                                        │
│    │                                                                        │
│    └──▶ 工作流 3 ──▶ 独立文件系统 3                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 设计方案
采用**侧边栏 + 拖拽创建节点**的方案：
- 左侧：文件系统树形结构（当前工作流的文件）
- 中间：React Flow 画布
- 右侧：文件/节点属性编辑器
- 拖拽文件到画布自动创建文件节点

---

## 二、系统架构

### 2.1 整体架构图

```
┌────────────────────────────────────────────────────────────────────────────┐
│                              前端 (React + Vite)                            │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                          工作流列表页                                  │  │
│  │  - 显示用户所有工作流                                                  │  │
│  │  - 创建/删除工作流                                                     │  │
│  │  - 使用模板创建工作流                                                  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                    │                                        │
│                                    ▼                                        │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                          工作流编辑页                                  │  │
│  │                                                                       │  │
│  │  ┌────────────┐  ┌────────────────────────────────────────────────┐  │  │
│  │  │            │  │                                                │  │  │
│  │  │ 文件系统    │  │            React Flow 画布                     │  │  │
│  │  │ (当前工作流) │  │                                                │  │  │
│  │  │            │  │   ┌────────┐     ┌────────┐     ┌────────┐    │  │  │
│  │  │ 📁 配置    │  │   │📄 文件 │────▶│🤖 AI  │────▶│📝 文件 │    │  │  │
│  │  │   📄世界观 │  │   │ Reader │     │ Node  │     │ Writer │    │  │  │
│  │  │   📄人物  │  │   └────────┘     └────────┘     └────────┘    │  │  │
│  │  │ 📁 章节    │  │                                                │  │  │
│  │  │   📄第1章 │  │                                                │  │  │
│  │  │            │  │                                                │  │  │
│  │  └────────────┘  └────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  │  ┌────────────────────────────────────────────────────────────────┐  │  │
│  │  │                    右侧 Inspector                              │  │  │
│  │  │  (文件内容编辑 / 节点配置)                                       │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
├────────────────────────────────────────────────────────────────────────────┤
│                              后端 (FastAPI)                                 │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │  工作流 API  │  │  文件 API   │  │  模板 API   │  │  执行引擎   │       │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                                             │
├────────────────────────────────────────────────────────────────────────────┤
│                              数据库 (PostgreSQL)                            │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │  users 表   │  │  flows 表   │  │  files 表   │  │ templates  │       │
│  │             │  │             │  │             │  │    表       │       │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘       │
│                                                                             │
│  关系: users 1:N flows 1:N files                                           │
│                                                                             │
└────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层级 | 技术 | 用途 |
|-----|------|-----|
| 前端框架 | React 18 + TypeScript | UI 构建 |
| 构建工具 | Vite | 开发和打包 |
| 路由 | React Router | 页面路由 |
| 样式 | Tailwind CSS | UI 样式 |
| 状态管理 | Zustand | 全局状态 |
| 文件树组件 | react-arborist 或自研 | 文件树渲染 |
| 工作流画布 | React Flow | 节点编辑器 |
| 拖拽功能 | HTML5 Drag & Drop API | 文件拖拽 |
| 编辑器 | Monaco Editor / SimpleMDE | Markdown 编辑 |
| 后端框架 | FastAPI | REST API |
| ORM | SQLAlchemy | 数据库操作 |
| 数据库 | PostgreSQL | 数据存储 |
| 迁移工具 | Alembic | 数据库版本管理 |

---

## 三、数据库设计

### 3.1 实体关系图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   users     │       │   flows     │       │   files     │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │◀──┐   │ id          │◀──┐   │ id          │
│ username    │   │   │ name        │   │   │ flow_id     │──▶ flows.id
│ email       │   │   │ description │   │   │ parent_id   │──▶ files.id
│ password    │   │   │ owner_id    │───┘   │ name        │
│ ...         │   │   │ nodes       │       │ type        │
└─────────────┘   │   │ edges       │       │ content     │
                  │   │ created_at  │       │ created_at  │
                  │   │ updated_at  │       │ updated_at  │
                  │   └─────────────┘       └─────────────┘
                  │                                       
                  └─── 一个用户可以有多个工作流
                                          
                  ┌─────────────┐       ┌─────────────┐
                  │   flows     │◀──────│flow_templates│
                  ├─────────────┤       ├─────────────┤
                  │             │       │ id          │
                  │             │       │ name        │
                  │             │       │ nodes       │
                  │             │       │ edges       │
                  │             │       │ files       │──▶ 文件快照
                  │             │       │ author_id   │
                  └─────────────┘       └─────────────┘
```

### 3.2 表结构定义

#### 3.2.1 工作流表 (flows)

```sql
CREATE TABLE flows (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    owner_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    
    -- 工作流定义
    nodes JSONB NOT NULL DEFAULT '[]',
    edges JSONB NOT NULL DEFAULT '[]',
    
    -- 元数据
    is_active BOOLEAN DEFAULT true,
    last_executed_at TIMESTAMP WITH TIME ZONE,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_flows_owner_id ON flows(owner_id);
```

#### 3.2.2 文件表 (files) - 归属工作流

```sql
CREATE TABLE files (
    id SERIAL PRIMARY KEY,
    flow_id INTEGER NOT NULL REFERENCES flows(id) ON DELETE CASCADE,
    parent_id INTEGER REFERENCES files(id) ON DELETE CASCADE,
    name VARCHAR(255) NOT NULL,
    type VARCHAR(20) NOT NULL,  -- 'file' | 'folder'
    content TEXT,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_files_flow_id ON files(flow_id);
CREATE INDEX idx_files_parent_id ON files(parent_id);
```

#### 3.2.3 工作流模板表 (flow_templates)

```sql
CREATE TABLE flow_templates (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    author_id INTEGER REFERENCES users(id),
    
    -- 工作流定义
    nodes JSONB NOT NULL,
    edges JSONB NOT NULL,
    
    -- 文件快照（包含文件内容）
    files JSONB NOT NULL DEFAULT '[]',
    
    -- 元数据
    category VARCHAR(100),
    tags TEXT[],
    use_count INTEGER DEFAULT 0,
    is_public BOOLEAN DEFAULT true,
    
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
```

#### 3.2.4 执行记录表 (executions)

```sql
CREATE TABLE executions (
    id SERIAL PRIMARY KEY,
    flow_id INTEGER NOT NULL REFERENCES flows(id) ON DELETE CASCADE,
    status VARCHAR(20) NOT NULL,  -- 'pending' | 'running' | 'success' | 'error'
    result JSONB,
    logs JSONB,
    error_message TEXT,
    
    started_at TIMESTAMP WITH TIME ZONE,
    completed_at TIMESTAMP WITH TIME ZONE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_executions_flow_id ON executions(flow_id);
```

### 3.3 SQLAlchemy 模型

```python
# app/modules/flows/models.py
from sqlalchemy import Column, Integer, String, Text, Boolean, ForeignKey, DateTime
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from app.core.database import Base

class Flow(Base):
    __tablename__ = "flows"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(255), nullable=False)
    description = Column(Text)
    owner_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    nodes = Column(JSONB, default=list)
    edges = Column(JSONB, default=list)
    is_active = Column(Boolean, default=True)
    last_executed_at = Column(DateTime(timezone=True))
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())

    owner = relationship("User", backref="flows")
    files = relationship("File", back_populates="flow", cascade="all, delete-orphan")
    executions = relationship("Execution", back_populates="flow", cascade="all, delete-orphan")


# app/modules/files/models.py
class File(Base):
    __tablename__ = "files"

    id = Column(Integer, primary_key=True, index=True)
    flow_id = Column(Integer, ForeignKey("flows.id", ondelete="CASCADE"), nullable=False)
    parent_id = Column(Integer, ForeignKey("files.id", ondelete="CASCADE"), nullable=True)
    name = Column(String(255), nullable=False)
    type = Column(String(20), nullable=False)
    content = Column(Text, nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())

    flow = relationship("Flow", back_populates="files")
    parent = relationship("File", remote_side=[id], backref="children")


# app/modules/templates/models.py
class FlowTemplate(Base):
    __tablename__ = "flow_templates"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(255), nullable=False)
    description = Column(Text)
    author_id = Column(Integer, ForeignKey("users.id"))
    nodes = Column(JSONB, nullable=False)
    edges = Column(JSONB, nullable=False)
    files = Column(JSONB, default=list)
    category = Column(String(100))
    tags = Column(JSONB, default=list)
    use_count = Column(Integer, default=0)
    is_public = Column(Boolean, default=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())

    author = relationship("User", backref="templates")
```

---

## 四、后端 API 设计

### 4.1 工作流 API

| 方法 | 路径 | 描述 |
|-----|------|-----|
| GET | `/api/flows` | 获取用户的所有工作流 |
| POST | `/api/flows` | 创建新工作流 |
| GET | `/api/flows/{flow_id}` | 获取工作流详情 |
| PUT | `/api/flows/{flow_id}` | 更新工作流 |
| DELETE | `/api/flows/{flow_id}` | 删除工作流（级联删除文件） |
| POST | `/api/flows/{flow_id}/execute` | 执行工作流 |
| POST | `/api/flows/{flow_id}/save-as-template` | 保存为模板 |

### 4.2 文件 API（归属工作流）

| 方法 | 路径 | 描述 |
|-----|------|-----|
| GET | `/api/flows/{flow_id}/files` | 获取工作流的文件树 |
| POST | `/api/flows/{flow_id}/files` | 创建文件/文件夹 |
| GET | `/api/flows/{flow_id}/files/{file_id}` | 获取文件详情 |
| PUT | `/api/flows/{flow_id}/files/{file_id}` | 更新文件 |
| DELETE | `/api/flows/{flow_id}/files/{file_id}` | 删除文件 |
| POST | `/api/flows/{flow_id}/files/{file_id}/move` | 移动文件 |

### 4.3 模板 API

| 方法 | 路径 | 描述 |
|-----|------|-----|
| GET | `/api/templates` | 获取模板列表 |
| POST | `/api/templates` | 创建模板 |
| GET | `/api/templates/{template_id}` | 获取模板详情 |
| POST | `/api/templates/{template_id}/use` | 使用模板创建工作流 |
| DELETE | `/api/templates/{template_id}` | 删除模板 |

### 4.4 API 详细设计

#### 4.4.1 创建工作流

```
POST /api/flows

Request:
{
  "name": "我的小说创作",
  "description": "自动生成小说章节"
}

Response:
{
  "id": 1,
  "name": "我的小说创作",
  "description": "自动生成小说章节",
  "owner_id": 1,
  "nodes": [],
  "edges": [],
  "created_at": "2024-01-01T00:00:00Z"
}
```

#### 4.4.2 获取工作流的文件树

```
GET /api/flows/{flow_id}/files

Response:
{
  "files": [
    {
      "id": 1,
      "name": "配置文件",
      "type": "folder",
      "parent_id": null,
      "children": [
        {
          "id": 2,
          "name": "世界观.md",
          "type": "file",
          "parent_id": 1
        }
      ]
    }
  ]
}
```

#### 4.4.3 创建文件

```
POST /api/flows/{flow_id}/files

Request:
{
  "name": "世界观.md",
  "type": "file",
  "parent_id": 1,
  "content": "# 世界观设定\n\n..."
}

Response:
{
  "id": 10,
  "flow_id": 1,
  "name": "世界观.md",
  "type": "file",
  "parent_id": 1,
  "content": "# 世界观设定\n\n...",
  "created_at": "2024-01-01T00:00:00Z"
}
```

#### 4.4.4 保存为模板

```
POST /api/flows/{flow_id}/save-as-template

Request:
{
  "name": "小说创作模板",
  "description": "基于配置文件自动生成小说章节",
  "include_files": true,        // 是否包含文件
  "selected_file_ids": [1,2,3], // 可选：指定要包含的文件
  "is_public": true
}

Response:
{
  "id": 1,
  "name": "小说创作模板",
  "description": "基于配置文件自动生成小说章节",
  "nodes": [...],
  "edges": [...],
  "files": [
    {
      "id": "file-1",
      "name": "世界观.md",
      "path": "/配置文件/世界观.md",
      "content": "# 世界观设定\n\n..."
    }
  ],
  "created_at": "2024-01-01T00:00:00Z"
}
```

#### 4.4.5 使用模板创建工作流

```
POST /api/templates/{template_id}/use

Request:
{
  "name": "我的小说创作",      // 可选，默认使用模板名称
  "copy_files": true          // 是否复制文件
}

Response:
{
  "flow": {
    "id": 10,
    "name": "我的小说创作",
    "nodes": [...],
    "edges": [...]
  },
  "files": [
    {
      "id": 100,
      "name": "世界观.md",
      "flow_id": 10
    }
  ],
  "message": "工作流创建成功，已复制 3 个文件"
}
```

---

## 五、前端组件设计

### 5.1 页面结构

```
src/
├── pages/
│   ├── Dashboard.tsx           # 工作流列表页
│   ├── FlowEditor.tsx          # 工作流编辑页
│   └── TemplateLibrary.tsx     # 模板库页面
│
├── components/
│   ├── FlowList/               # 工作流列表组件
│   │   ├── index.tsx
│   │   ├── FlowCard.tsx
│   │   └── CreateFlowDialog.tsx
│   │
│   ├── FileExplorer/           # 文件系统组件
│   │   ├── index.tsx
│   │   ├── FileTree.tsx
│   │   ├── FileItem.tsx
│   │   ├── FolderItem.tsx
│   │   ├── ContextMenu.tsx
│   │   └── CreateFileDialog.tsx
│   │
│   ├── FlowCanvas/             # 工作流画布组件
│   │   ├── index.tsx
│   │   ├── nodes/
│   │   │   ├── FileReaderNode.tsx
│   │   │   ├── FileWriterNode.tsx
│   │   │   ├── AINode.tsx
│   │   │   └── InputNode.tsx
│   │   └── DropZone.tsx
│   │
│   ├── Inspector/              # 右侧属性面板
│   │   ├── index.tsx
│   │   ├── FileEditor.tsx
│   │   └── NodeConfig.tsx
│   │
│   └── TemplateList/           # 模板列表组件
│       ├── index.tsx
│       └── TemplateCard.tsx
│
├── store/
│   ├── flowStore.ts            # 工作流状态
│   ├── fileStore.ts            # 文件系统状态
│   └── templateStore.ts        # 模板状态
│
└── types/
    ├── flow.ts
    ├── file.ts
    └── template.ts
```

### 5.2 核心页面设计

#### 5.2.1 工作流列表页 (Dashboard)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  AI 文本工作流                                           用户名 ▼  退出      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📋 我的工作流                              [+ 新建工作流]            │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                     │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │   │
│  │  │ 📖 小说创作  │  │ 📝 文案生成  │  │ 📊 报告总结  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ 12 个文件   │  │ 5 个文件    │  │ 8 个文件    │                │   │
│  │  │ 上次: 2小时前│  │ 上次: 1天前  │  │ 上次: 3天前  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ [打开] [删除]│  │ [打开] [删除]│  │ [打开] [删除]│                │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📚 模板库                                                           │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                     │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │   │
│  │  │ 📖 小说创作  │  │ 📝 营销文案  │  │ 📊 会议纪要  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ 作者: 张三  │  │ 作者: 李四  │  │ 作者: 王五  │                │   │
│  │  │ 使用: 128次 │  │ 使用: 56次  │  │ 使用: 89次  │                │   │
│  │  │             │  │             │  │             │                │   │
│  │  │ [使用模板]  │  │ [使用模板]  │  │ [使用模板]  │                │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘                │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 5.2.2 工作流编辑页 (FlowEditor)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  📖 小说创作工作流                    [保存] [执行] [保存为模板]  [返回]     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────┐  ┌─────────────────────────────────────────────────────┐ │
│  │              │  │                                                     │ │
│  │  📁 文件系统  │  │              React Flow 画布                         │ │
│  │  (此工作流)   │  │                                                     │ │
│  │              │  │   ┌─────────┐     ┌─────────┐     ┌─────────┐       │ │
│  │  📁 配置文件  │  │   │📄 文件  │────▶│🤖 AI   │────▶│📝 文件  │       │ │
│  │    📄世界观   │  │   │ Reader  │     │ Node   │     │ Writer  │       │ │
│  │    📄人物    │  │   └─────────┘     └─────────┘     └─────────┘       │ │
│  │  📁 章节文件  │  │                                                     │ │
│  │    📄第1章   │  │                                                     │ │
│  │              │  │                                                     │ │
│  │  (拖拽文件到  │  │                                                     │ │
│  │   画布创建    │  │                                                     │ │
│  │   节点)      │  │                                                     │ │
│  │              │  │                                                     │ │
│  └──────────────┘  └─────────────────────────────────────────────────────┘ │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  属性面板 (选中文件/节点时显示)                                        │   │
│  │  - 文件：显示内容编辑器                                                │   │
│  │  - 节点：显示配置表单                                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 六、状态管理设计

### 6.1 flowStore

```typescript
interface FlowStore {
  // 状态
  flows: Flow[];
  currentFlow: Flow | null;
  loading: boolean;
  error: string | null;

  // 工作流操作
  fetchFlows: () => Promise<void>;
  createFlow: (data: CreateFlowDto) => Promise<Flow>;
  updateFlow: (id: number, data: UpdateFlowDto) => Promise<Flow>;
  deleteFlow: (id: number) => Promise<void>;
  setCurrentFlow: (flow: Flow | null) => void;
  
  // 节点操作
  nodes: Node[];
  edges: Edge[];
  addNode: (node: Node) => void;
  updateNode: (id: string, data: any) => void;
  deleteNode: (id: string) => void;
  addEdge: (edge: Edge) => void;
  deleteEdge: (id: string) => void;
  
  // 文件节点
  createFileReaderNode: (file: File, position: XYPosition) => void;
  createFileWriterNode: (file: File, position: XYPosition) => void;
  
  // 模板
  saveAsTemplate: (data: SaveTemplateDto) => Promise<void>;
  createFromTemplate: (templateId: number, data: CreateFromTemplateDto) => Promise<Flow>;
}
```

### 6.2 fileStore

```typescript
interface FileStore {
  // 状态
  files: File[];
  selectedFileId: number | null;
  loading: boolean;
  error: string | null;

  // 操作（都需要 flow_id）
  fetchFiles: (flowId: number) => Promise<void>;
  createFile: (flowId: number, data: CreateFileDto) => Promise<File>;
  updateFile: (flowId: number, fileId: number, data: UpdateFileDto) => Promise<File>;
  deleteFile: (flowId: number, fileId: number) => Promise<void>;
  moveFile: (flowId: number, fileId: number, parentId: number | null) => Promise<void>;
  selectFile: (id: number | null) => void;
}
```

---

## 七、模板机制设计

### 7.1 保存为模板流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           保存为模板                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Step 1: 用户点击"保存为模板"                                                 │
│                                                                             │
│  Step 2: 系统扫描工作流中的文件节点                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  发现以下文件节点：                                                    │   │
│  │                                                                     │   │
│  │  📄 FileReader: 世界观.md (ID: 123)                                  │   │
│  │  📄 FileReader: 人物.md (ID: 456)                                    │   │
│  │  📝 FileWriter: 第{n}章.md (动态路径)                                 │   │
│  │                                                                     │   │
│  │  选择要包含的文件：                                                    │   │
│  │  ☑️ 世界观.md                                                        │   │
│  │  ☑️ 人物.md                                                         │   │
│  │  ☐ 第{n}章.md (动态文件，不包含)                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  Step 3: 生成模板数据                                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  {                                                                  │   │
│  │    "name": "小说创作工作流",                                          │   │
│  │    "nodes": [...],                                                  │   │
│  │    "edges": [...],                                                  │   │
│  │    "files": [                                                       │   │
│  │      {                                                              │   │
│  │        "id": "file-123",                                            │   │
│  │        "name": "世界观.md",                                          │   │
│  │        "path": "/配置文件/世界观.md",                                 │   │
│  │        "content": "# 世界观设定\n..."                                 │   │
│  │      }                                                              │   │
│  │    ]                                                                │   │
│  │  }                                                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  Step 4: 保存到模板库                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 使用模板创建工作流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        使用模板创建工作流                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Step 1: 用户选择模板，点击"使用模板"                                          │
│                                                                             │
│  Step 2: 系统创建工作流和文件副本                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  模板文件                创建到新工作流                                │   │
│  │  ─────────              ─────────────                               │   │
│  │  📄 世界观.md   ──▶      📄 世界观.md (新ID: 1001)                    │   │
│  │  📄 人物.md     ──▶      📄 人物.md (新ID: 1002)                      │   │
│  │                                                                     │   │
│  │  ✅ 文件已复制                                                        │   │
│  │  ✅ 工作流已创建                                                      │   │
│  │  ✅ 节点ID已映射                                                      │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  Step 3: 用户获得独立的工作流和文件系统                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 八、开发任务分解

### 阶段一：数据库与后端基础（预计 2-3 天）

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 1.1 | 创建 Flow 模型 | P0 |
| 1.2 | 创建 File 模型（归属 Flow） | P0 |
| 1.3 | 创建 FlowTemplate 模型 | P0 |
| 1.4 | 生成数据库迁移 | P0 |
| 1.5 | 实现工作流 CRUD API | P0 |
| 1.6 | 实现文件 CRUD API（带 flow_id） | P0 |
| 1.7 | 实现模板 API | P1 |

### 阶段二：前端工作流管理（预计 1-2 天）

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 2.1 | 创建 flowStore | P0 |
| 2.2 | 实现工作流列表页 | P0 |
| 2.3 | 实现创建工作流对话框 | P0 |
| 2.4 | 实现工作流卡片组件 | P1 |

### 阶段三：前端文件系统（预计 2-3 天）

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 3.1 | 更新 fileStore（支持 flow_id） | P0 |
| 3.2 | 实现 FileExplorer 组件 | P0 |
| 3.3 | 实现 FileTree 递归渲染 | P0 |
| 3.4 | 实现右键菜单 | P1 |
| 3.5 | 实现创建/重命名对话框 | P1 |

### 阶段四：工作流编辑器（预计 2-3 天）

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 4.1 | 创建 FlowEditor 页面 | P0 |
| 4.2 | 集成 React Flow | P0 |
| 4.3 | 创建 FileReaderNode | P0 |
| 4.4 | 创建 FileWriterNode | P0 |
| 4.5 | 实现拖拽创建节点 | P0 |
| 4.6 | 实现 Inspector 面板 | P1 |

### 阶段五：模板功能（预计 1-2 天）

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 5.1 | 实现保存为模板功能 | P0 |
| 5.2 | 实现模板列表页 | P0 |
| 5.3 | 实现使用模板创建工作流 | P0 |
| 5.4 | 实现文件 ID 映射逻辑 | P1 |

### 阶段六：整合与优化（预计 1 天）

| 任务 | 描述 | 优先级 |
|-----|------|-------|
| 6.1 | 整合所有页面 | P0 |
| 6.2 | 添加加载状态 | P1 |
| 6.3 | 错误处理优化 | P1 |
| 6.4 | 性能优化 | P2 |

---

## 九、注意事项

### 9.1 数据隔离

- 每个工作流的文件系统完全独立
- 删除工作流时级联删除所有文件
- 文件操作必须验证 flow_id 归属

### 9.2 性能考虑

- 文件树使用虚拟滚动（文件多时）
- 文件内容懒加载（点击时才加载）
- 自动保存使用防抖

### 9.3 安全考虑

- 所有操作验证用户权限
- 文件名过滤特殊字符
- 文件大小限制

---

## 十、后续扩展

- 文件版本历史
- 工作流分享功能
- 工作流导入/导出
- 协作编辑
- 文件模板
```

文档已更新完成！主要变更：

1. **架构调整**：文件系统归属工作流，每个工作流独立
2. **数据库设计**：files 表新增 flow_id 外键
3. **API 设计**：文件 API 路径改为 `/api/flows/{flow_id}/files`
4. **前端页面**：新增工作流列表页，文件系统显示当前工作流的文件
5. **模板机制**：完整的工作流复制功能，包括文件副本

准备好开始开发时告诉我！